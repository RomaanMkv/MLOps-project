data:
  path: ${hydra:runtime.cwd}/data_base/
  raw_data_name: sample
  data_format: .csv
  version: 1
  sample_size: 1
  sample_name: sample.csv
  sample_folder: ${hydra:runtime.cwd}/data/samples
gx:
  data_path: ${hydra:runtime.cwd}/data/samples/sample.csv
  project_root_dir: ${hydra:runtime.cwd}/services
  datasource_name: pandas_datasource
  asset_name: csv_file
  suite_name: initial_data_validation
